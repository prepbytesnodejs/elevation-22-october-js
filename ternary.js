// --->ternary operator --->

// (condition)? (expression if condition is true):(expression if condition is false)

//var num=21;

//num %2==0 ? console.log("its even"):console.log("its odd")

var age=16;

var status=age>14?(age>=18?"legal age to marry":"not a legal age to marrry"):(age <8?"you are a toddler":"you are not a toddler" );

//console.log(status); // not a legal age to marry

// conditional rendering in react --->